server:
  port: 8080


spring:
  datasource:
    username: root
    password: a1b2c3
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/simpleftp?serverTimezone=UTC&useUnicode=true@characterEncoding=utf-8

  resources:
    add-mappings: true

mybatis:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.xzit.simpleftp.entity
logging:
  level:
    com.xzit.simpleftp.mapper: trace
